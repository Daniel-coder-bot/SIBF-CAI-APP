
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // REGLAS PARA LA DEMO - PERMITE ACCESO TOTAL
    // Esto resuelve los errores de "Missing or insufficient permissions"
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
